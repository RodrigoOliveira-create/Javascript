let ValordoIngresso = 20;
ValordoIngresso = 30;
console.log(ValordoIngresso);
//O let permite que o valor da variavel mude, caso eu alterasse para const, geraria um erro, pois valores constantes não mudam, o ideal é sempre usar o const, mas caso os valores possam mudar use o let.
//Const ValordoIngresso = 20;
//ValordoIngresso = 30;
//console.log(ValordoIngresso);