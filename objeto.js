let familia = [25,45,'Rodrigo',17];
console.log(familia.length); // length ele conta quantas palavras, mas já que o primeiro é numero ele retorna o numero 25
console.log (familia[0]); //[0] seleciona a informação que quer que apareça 
let nomeDoColega = ['Ritsu', '40 anos','2 filhos'];
console.log (nomeDoColega); 
let corSite = "azul"; //Variavel que definimos na azul
function resetaCor(){  //função que reseta a cor
  corSite = "";        // Aqui eu defino que vai ser resetado ou coloco outra informação, isso me da a opção de zerar a informação ou acrescentar mais informação
};
console. log(corSite);
resetaCor();
console.log(corSite);
